# 生产环境配置示例
# 使用方法：
# 1. 复制此文件: cp .env.production.example .env
# 2. 修改下面的配置项（特别是标记为 [必须修改] 的项）
# 3. 生成安全的密码和密钥

# ===========================
# 应用配置
# ===========================
APP_ENV=production

# CORS 允许的来源（逗号分隔）
# [必须修改] 替换为实际的服务器 IP 或域名
# 示例: http://123.45.67.89 或 http://your-domain.com
# 如果同时使用 IP 和域名，都填上
APP_CORS_ORIGINS=http://YOUR_SERVER_IP

# ===========================
# 管理员配置
# ===========================
ADMIN_USERNAME=admin

# 管理员密码 - 两种方式二选一

# 方式一：直接设置明文密码（简单但不推荐）
# [必须修改] 设置强密码
ADMIN_PASSWORD=your-strong-password-here

# 方式二：使用哈希密码（推荐）
# 生成方法: python3 -c "import bcrypt; print(bcrypt.hashpw(b'your-password', bcrypt.gensalt()).decode())"
# ADMIN_PASSWORD_HASH=$2b$12$...

# ===========================
# JWT 认证配置
# ===========================
# [必须修改] JWT 签名密钥 - 必须是随机字符串，至少 32 个字符
# 生成方法: openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-secret-key-at-least-32-chars

# Token 过期时间（分钟）
# 720 分钟 = 12 小时
JWT_EXPIRE_MINUTES=720

# ===========================
# 数据库配置（可选）
# ===========================
# 默认使用 SQLite，数据库文件位于 backend/data/drinktea.db
# 如果使用 PostgreSQL，可以在代码中添加连接配置

# ===========================
# 文件上传配置（可选）
# ===========================
# 最大上传文件大小（字节）
# MAX_UPLOAD_SIZE=10485760

# ===========================
# 日志配置（可选）
# ===========================
# LOG_LEVEL=info
# LOG_FILE=/opt/drinktea/backend/logs/app.log

# ===========================
# 安全提示
# ===========================
# 1. 使用强密码（至少 16 个字符，包含大小写字母、数字、特殊字符）
# 2. 使用随机的 JWT_SECRET
# 3. 不要将 .env 文件提交到版本控制系统
# 4. 定期更新密码和密钥
# 5. 设置文件权限: chmod 600 .env
